# This workspace directory is mounted into the agent at runtime.
# Add any files here that you want the agent to have access to on startup.
# The agent can read and write files to this directory via its tools.
