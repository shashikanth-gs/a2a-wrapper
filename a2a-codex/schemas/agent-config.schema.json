{
  "$ref": "#/definitions/AgentConfig",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "AgentCardConfig": {
      "additionalProperties": false,
      "description": "Agent identity and capabilities advertised via the A2A Agent Card.\n\nThis interface captures all fields that appear in the public-facing agent card served at `/.well-known/agent-card.json`. Orchestrators and callers use this metadata to discover the agent's name, supported modes, streaming capability, and available skills.",
      "properties": {
        "defaultInputModes": {
          "default": [
            "text"
          ],
          "description": "Supported input content modes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "defaultOutputModes": {
          "default": [
            "text"
          ],
          "description": "Supported output content modes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "description": {
          "description": "Agent description shown to orchestrators and callers. Should concisely explain the agent's purpose and domain expertise.",
          "type": "string"
        },
        "name": {
          "description": "Human-readable agent name. Displayed in orchestrator UIs and agent discovery listings.",
          "type": "string"
        },
        "protocolVersion": {
          "default": "0.3.0",
          "deprecated": "Superseded by A2A v1.0's per-interface `protocolVersion`\n(see `AgentCard.supportedInterfaces[]`, built by `buildAgentCard`).\nKept for backward compatibility with existing config files only —\nthe agent card builder no longer reads this value.",
          "description": "A2A protocol version this agent supports.",
          "type": "string"
        },
        "provider": {
          "additionalProperties": false,
          "description": "Agent provider information. Identifies the organization responsible for this agent deployment.",
          "properties": {
            "organization": {
              "description": "Organization name.",
              "type": "string"
            },
            "url": {
              "description": "Optional URL for the provider's website or documentation.",
              "type": "string"
            }
          },
          "required": [
            "organization"
          ],
          "type": "object"
        },
        "pushNotifications": {
          "default": false,
          "description": "Whether the agent supports push notifications.",
          "type": "boolean"
        },
        "signing": {
          "additionalProperties": false,
          "description": "Optional Signed Agent Card support (A2A v1.0, JWS per RFC 7515). Off by default. When enabled, the private key is read from the environment variable named by `privateKeyJwkEnvVar`, whose value must be a JSON-serialized JWK — never store key material in the config file itself.",
          "properties": {
            "algorithm": {
              "description": "JWS algorithm (e.g. `\"RS256\"`, `\"ES256\"`).",
              "type": "string"
            },
            "enabled": {
              "default": false,
              "type": "boolean"
            },
            "keyId": {
              "description": "Key ID (`kid`) to embed in the JWS protected header.",
              "type": "string"
            },
            "privateKeyJwkEnvVar": {
              "description": "Name of the env var holding the signing private key as a JSON JWK.",
              "type": "string"
            }
          },
          "required": [
            "privateKeyJwkEnvVar",
            "keyId",
            "algorithm"
          ],
          "type": "object"
        },
        "skills": {
          "description": "Skills this agent exposes to callers. Each skill represents a discrete capability advertised in the agent card.",
          "items": {
            "$ref": "#/definitions/SkillConfig"
          },
          "type": "array"
        },
        "stateTransitionHistory": {
          "default": false,
          "deprecated": "This capability is not implemented in the A2A v1.0 spec and\nwas removed. Kept for backward compatibility only — value is ignored.\nThe agent card builder always sets this to `false`.",
          "description": "Enable state transition history capability advertisement.",
          "type": "boolean"
        },
        "streaming": {
          "default": true,
          "description": "Whether the agent supports streaming responses.",
          "type": "boolean"
        },
        "version": {
          "default": "1.0.0",
          "description": "Agent software version string.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "description"
      ],
      "type": "object"
    },
    "AgentConfig": {
      "additionalProperties": false,
      "description": "Complete agent configuration.\n\nThis is what a JSON config file (e.g. `agents/example/config.json`) maps to. All fields except `agentCard` are optional — sensible secure defaults are applied.",
      "properties": {
        "agentCard": {
          "$ref": "#/definitions/AgentCardConfig"
        },
        "codex": {
          "$ref": "#/definitions/CodexConfig",
          "description": "Codex SDK connection and execution settings."
        },
        "configDir": {
          "description": "Directory containing the agent's config.json file. Populated automatically by the CLI loader. Do not set manually.",
          "type": "string"
        },
        "events": {
          "$ref": "#/definitions/EventsConfig"
        },
        "features": {
          "$ref": "#/definitions/FeatureFlags"
        },
        "logging": {
          "$ref": "#/definitions/LoggingConfig"
        },
        "mcp": {
          "additionalProperties": {
            "$ref": "#/definitions/McpServerConfig"
          },
          "description": "MCP servers to register with Codex at startup. Keys are server names; values are stdio or http server configs. The key \"a2a-subagents\" is reserved for the sub-agent bridge.",
          "type": "object"
        },
        "memory": {
          "$ref": "#/definitions/MemoryConfig"
        },
        "server": {
          "$ref": "#/definitions/ServerConfig"
        },
        "session": {
          "$ref": "#/definitions/SessionConfig"
        },
        "subAgents": {
          "$ref": "#/definitions/SubAgentsConfig"
        },
        "timeouts": {
          "$ref": "#/definitions/TimeoutConfig"
        }
      },
      "required": [
        "agentCard"
      ],
      "type": "object"
    },
    "CodexConfig": {
      "additionalProperties": false,
      "description": "OpenAI Codex SDK connection and execution settings.\n\nAll fields map to",
      "properties": {
        "additionalDirectories": {
          "description": "Additional directories to make accessible to Codex beyond workingDirectory. Each path supports ${ENV_VAR} substitution. Use with care.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "approvalPolicy": {
          "default": "never",
          "description": "Tool approval policy for shell commands and MCP tool calls.\n- \"never\"      — auto-approve all (recommended for headless A2A operation)\n- \"on-failure\" — approve only on command failure\n- \"untrusted\"  — approve tools from untrusted sources\n- \"on-request\" — blocked; interactive approvals are incompatible with headless A2A",
          "enum": [
            "never",
            "on-request",
            "on-failure",
            "untrusted"
          ],
          "type": "string"
        },
        "baseUrl": {
          "description": "Override the OpenAI API base URL (useful for corporate proxies).",
          "type": "string"
        },
        "codexPathOverride": {
          "description": "Override the path to the Codex CLI binary.",
          "type": "string"
        },
        "configOverrides": {
          "additionalProperties": {},
          "description": "Additional Codex configuration overrides passed as CodexOptions.config. Mapped to --config key=value CLI arguments. See Codex config reference.",
          "type": "object"
        },
        "contextFile": {
          "default": "context.md",
          "description": "Filename for the pre-built domain context file within workingDirectory.",
          "type": "string"
        },
        "contextPrompt": {
          "description": "Default prompt used when buildContext() is called without an explicit prompt.",
          "type": "string"
        },
        "developerInstructions": {
          "description": "Instructions prepended to every Codex prompt as developer context. Use to enforce coding standards, tool usage rules, and safety constraints.",
          "type": "string"
        },
        "model": {
          "description": "Model to use (e.g. \"o4-mini\", \"gpt-4o\"). Falls back to Codex default when omitted. Supports ${CODEX_MODEL} environment variable substitution.",
          "type": "string"
        },
        "networkAccessEnabled": {
          "default": false,
          "description": "Allow Codex to make outbound network requests. Disabled by default for security. Enable only when explicitly required.",
          "type": "boolean"
        },
        "sandboxMode": {
          "default": "workspace-write",
          "description": "Codex sandbox mode controlling filesystem access.\n- \"read-only\"          — analysis and review tasks; no file writes\n- \"workspace-write\"    — recommended default; writes scoped to workingDirectory\n- \"danger-full-access\" — unrestricted; requires explicit opt-in and logs a warning",
          "enum": [
            "read-only",
            "workspace-write",
            "danger-full-access"
          ],
          "type": "string"
        },
        "skipGitRepoCheck": {
          "default": false,
          "description": "Skip Codex's built-in Git repository validation check. Leave false to ensure the workspace is a real git repo.",
          "type": "boolean"
        },
        "webSearchMode": {
          "default": "disabled",
          "description": "Web search access for Codex.\n- \"disabled\" — no web search (default, secure)\n- \"cached\"   — use cached search results only\n- \"live\"     — live web search (requires networkAccessEnabled: true)",
          "enum": [
            "disabled",
            "cached",
            "live"
          ],
          "type": "string"
        },
        "workingDirectory": {
          "description": "Absolute path to the Git repository Codex should operate on. Required at runtime (validated by the executor). Supports ${ENV_VAR} substitution.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "EventsConfig": {
      "additionalProperties": false,
      "description": "Event transport configuration for sideband observability events.\n\nControls how trace events (MCP tool calls, agent reasoning, lifecycle) are delivered. Only the two built-in transports are configurable via JSON config — for custom transports (Kafka, Redis, DB, etc.), use the programmatic API and pass a transport function to `createA2AServer()`.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable event emission.",
          "type": "boolean"
        },
        "httpHeaders": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Custom HTTP headers sent with every event POST. Useful for authentication tokens: ```json { \"Authorization\": \"Bearer ${CORTEX_TOKEN}\" } ```",
          "type": "object"
        },
        "httpTimeout": {
          "default": "10_000",
          "description": "HTTP request timeout in milliseconds for the `\"http\"` transport.",
          "type": "number"
        },
        "httpUrl": {
          "description": "HTTP endpoint URL for the `\"http\"` transport. Required when `transport` is `\"http\"`.",
          "type": "string"
        },
        "transport": {
          "default": "a2a",
          "description": "Built-in transport type.\n\n- `\"a2a\"` — publish as sideband trace artifacts on the A2A   ExecutionEventBus (default, zero dependencies).\n- `\"http\"` — POST events as JSON to  {@link  httpUrl } .\n\nFor custom transports, leave this unset and provide a transport function via the programmatic `createA2AServer()` API.",
          "enum": [
            "a2a",
            "http"
          ],
          "type": "string"
        }
      },
      "type": "object"
    },
    "FeatureFlags": {
      "additionalProperties": false,
      "properties": {
        "emitCommandEvents": {
          "description": "Publish shell command start/end as tool_call sideband events. Default: true.",
          "type": "boolean"
        },
        "emitFileChangeEvents": {
          "description": "Publish file change metadata as sideband events. Default: true.",
          "type": "boolean"
        },
        "emitReasoningSummaries": {
          "description": "Publish reasoning summaries as thinking sideband events. Default: true.",
          "type": "boolean"
        },
        "streamArtifactChunks": {
          "description": "Stream artifact chunks individually (A2A spec-correct) vs buffer and send single artifact (inspector-compatible). Default: false (buffered).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "LoggingConfig": {
      "additionalProperties": false,
      "properties": {
        "level": {
          "type": "string"
        }
      },
      "type": "object"
    },
    "McpHttpServerConfig": {
      "additionalProperties": false,
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "HTTP headers sent with every request. Values support ${ENV_VAR} substitution. Use for bearer tokens: { \"Authorization\": \"Bearer ${TOKEN}\" }",
          "type": "object"
        },
        "toolTimeoutSec": {
          "type": "number"
        },
        "type": {
          "const": "http",
          "type": "string"
        },
        "url": {
          "description": "URL of the Streamable HTTP MCP server.",
          "type": "string"
        }
      },
      "required": [
        "type",
        "url"
      ],
      "type": "object"
    },
    "McpServerConfig": {
      "anyOf": [
        {
          "$ref": "#/definitions/McpStdioServerConfig"
        },
        {
          "$ref": "#/definitions/McpHttpServerConfig"
        },
        {
          "additionalProperties": {},
          "properties": {
            "type": {
              "type": "string"
            }
          },
          "required": [
            "type"
          ],
          "type": "object"
        }
      ]
    },
    "McpStdioServerConfig": {
      "additionalProperties": false,
      "properties": {
        "args": {
          "description": "Arguments. Values support ${ENV_VAR} substitution.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command": {
          "description": "Command to launch the MCP server.",
          "type": "string"
        },
        "disabledTools": {
          "description": "Denylist of tool names to block.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "type": "boolean"
        },
        "enabledTools": {
          "description": "Allowlist of tool names to expose. If set, only these tools are accessible.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Environment variables for the spawned process. Values support ${ENV_VAR} substitution.",
          "type": "object"
        },
        "startupTimeoutSec": {
          "description": "MCP server startup timeout in seconds.",
          "type": "number"
        },
        "toolTimeoutSec": {
          "description": "Per-tool call timeout in seconds.",
          "type": "number"
        },
        "type": {
          "const": "stdio",
          "type": "string"
        }
      },
      "required": [
        "type",
        "command"
      ],
      "type": "object"
    },
    "MemoryConfig": {
      "additionalProperties": false,
      "description": "Optional memory configuration section in agent config.json.\n\nDeclares instructions and skills to materialize into the agent's workspace at startup. Both fields are optional — operators can declare instructions only, skills only, or both.\n\nPaths are resolved relative to the directory containing config.json (Config_Directory) unless they are absolute.",
      "properties": {
        "instructions": {
          "description": "Path to a markdown instructions file. Resolved relative to Config_Directory when relative, used as-is when absolute.",
          "type": "string"
        },
        "skills": {
          "description": "Paths to skill directories, each containing a SKILL.md file. Resolved relative to Config_Directory when relative, used as-is when absolute.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ServerConfig": {
      "additionalProperties": false,
      "properties": {
        "advertiseHost": {
          "description": "Hostname advertised in agent card URLs (default: \"localhost\"). Set to machine IP or \"host.containers.internal\" for Docker.",
          "type": "string"
        },
        "advertiseProtocol": {
          "description": "Protocol used in advertised URLs (default: \"http\"). Set to \"https\" when deployed behind TLS or a TLS-terminating reverse proxy.",
          "enum": [
            "http",
            "https"
          ],
          "type": "string"
        },
        "hostname": {
          "type": "string"
        },
        "port": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "SessionConfig": {
      "additionalProperties": false,
      "properties": {
        "cleanupInterval": {
          "description": "Session cleanup interval in ms (default: 300_000 = 5 min)",
          "type": "number"
        },
        "reuseByContext": {
          "description": "Reuse sessions by A2A contextId (default: true)",
          "type": "boolean"
        },
        "titlePrefix": {
          "type": "string"
        },
        "ttl": {
          "description": "Session TTL in ms (default: 3_600_000 = 1 hour)",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SkillConfig": {
      "additionalProperties": false,
      "description": "A single skill exposed on the agent card.\n\nSkills describe discrete capabilities the agent advertises to orchestrators and callers via the A2A Agent Card. Each skill has a unique identifier, human-readable metadata, and optional usage examples.",
      "properties": {
        "description": {
          "description": "Detailed description of what this skill does. Helps orchestrators decide whether to route a task to this agent.",
          "type": "string"
        },
        "examples": {
          "description": "Optional example prompts demonstrating how to invoke this skill. Included in the agent card only when non-empty.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "id": {
          "description": "Unique identifier for this skill. Used as the stable key when referencing the skill programmatically.",
          "type": "string"
        },
        "name": {
          "description": "Human-readable display name for the skill. Shown in agent card UIs and orchestrator dashboards.",
          "type": "string"
        },
        "tags": {
          "description": "Optional tags for categorization and discovery. Orchestrators may use tags to filter or group agents by capability.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ],
      "type": "object"
    },
    "SubAgentAuthConfig": {
      "anyOf": [
        {
          "additionalProperties": false,
          "properties": {
            "mode": {
              "const": "none",
              "type": "string"
            }
          },
          "required": [
            "mode"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "mode": {
              "const": "bearer",
              "type": "string"
            },
            "token": {
              "type": "string"
            }
          },
          "required": [
            "mode",
            "token"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "headerName": {
              "type": "string"
            },
            "mode": {
              "const": "api_key",
              "type": "string"
            },
            "token": {
              "type": "string"
            }
          },
          "required": [
            "mode",
            "token"
          ],
          "type": "object"
        }
      ],
      "description": "Outbound credentials the bridge presents when calling a remote A2A sub-agent. Forwarded verbatim into the bridge config consumed by `a2a-mcp-skillmap`.\n\n`mode: \"none\"` is equivalent to omitting the `auth` field entirely and produces no auth block in the generated bridge config.\n\nFor `mode: \"api_key\"`, the optional `headerName` defaults to whatever `a2a-mcp-skillmap` uses (typically `X-API-Key`).\n\nTokens may reference environment variables via `${VAR_NAME}` syntax; the substitution happens during validation and missing variables cause the auth block to be omitted with a warning."
    },
    "SubAgentConfig": {
      "additionalProperties": false,
      "description": "One remote A2A agent the parent will expose as MCP tools via the `a2a-mcp-skillmap` bridge.",
      "properties": {
        "agentCardUrl": {
          "description": "URL of the sub-agent's A2A agent card. Required.\n\nTypically `https://<host>/.well-known/agent-card.json`, but any URL the bridge can probe is valid. Must use the `http:` or `https:` protocol.",
          "type": "string"
        },
        "auth": {
          "$ref": "#/definitions/SubAgentAuthConfig",
          "description": "Outbound credentials the bridge presents when calling this sub-agent. Forwarded verbatim into the bridge config.\n\nOmitting this field is equivalent to `{ mode: \"none\" }`."
        },
        "endpointUrlOverride": {
          "description": "Optional override for the URL the bridge probes. Useful when the agent card declares an endpoint that differs from where the agent is actually reachable (NAT, port-forward, internal vs external).\n\nWhen set, this URL is passed to the bridge instead of  {@link  agentCardUrl } . The bridge still resolves a card from this URL — point it at a card URL or a base URL it can probe under `/.well-known/`. Must use the `http:` or `https:` protocol.",
          "type": "string"
        },
        "name": {
          "description": "Stable identifier for this sub-agent. Becomes the prefix on generated MCP tool names (`{name}__{skillId}`). Required.\n\nAllowed characters: ASCII letters, digits, hyphens, and underscores (must match `/^[A-Za-z0-9_-]+$/`). Must be unique across all entries in the `subAgents.agents` array.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "agentCardUrl"
      ],
      "type": "object"
    },
    "SubAgentsConfig": {
      "additionalProperties": false,
      "description": "Top-level shape of the `subAgents` field merged into the agent's `BaseAgentConfig`.\n\nWhen this field is absent or `agents` is an empty array, the parent skips every sub-agent code path with no side effects (no bridge config written, no MCP entry injected).",
      "properties": {
        "agents": {
          "description": "The list of remote A2A agents to expose as MCP tools.",
          "items": {
            "$ref": "#/definitions/SubAgentConfig"
          },
          "type": "array"
        },
        "options": {
          "$ref": "#/definitions/SubAgentsOptions",
          "description": "Optional bridge-wide knobs."
        }
      },
      "required": [
        "agents"
      ],
      "type": "object"
    },
    "SubAgentsOptions": {
      "additionalProperties": false,
      "description": "Optional knobs that apply to the bridge process as a whole, not to any single sub-agent.",
      "properties": {
        "probeTimeoutMs": {
          "description": "Probe timeout in milliseconds for the startup reachability check the parent performs against each sub-agent. Defaults to `5000` when unset.",
          "type": "number"
        },
        "responseMode": {
          "description": "Skillmap response shaping mode passed through to the bridge config. Defaults to `\"artifact\"` when unset.",
          "enum": [
            "structured",
            "compact",
            "artifact",
            "raw"
          ],
          "type": "string"
        },
        "syncBudgetMs": {
          "description": "Sync budget in milliseconds passed to `a2a-mcp-skillmap` via the bridge config. Controls how long the bridge waits for an A2A agent to respond before switching to async task-handle mode.\n\nWhen the budget expires the bridge immediately returns a `taskId` to the MCP client; the A2A dispatch continues in the background. The LLM can then poll via the built-in `task_result` / `task_status` tools which actively re-query the remote agent before responding.\n\n- Set lower (e.g. `10000`) for interactive use where you want fast   task-handle responses.\n- Set to `0` to wait indefinitely (no async fallback).\n- Defaults to `30000` (30 s) when unset — skillmap's own default.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "TimeoutConfig": {
      "additionalProperties": false,
      "properties": {
        "prompt": {
          "description": "Timeout for a single prompt in ms (default: 600_000 = 10 min)",
          "type": "number"
        }
      },
      "type": "object"
    }
  }
}